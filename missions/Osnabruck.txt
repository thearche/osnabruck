osnabruck_munster_conquest = {
	slot = 1
	generic = no
	ai = yes
	potential = {
		tag = OSN
		NOT = { map_setup = map_setup_random }
	}
	has_country_shield = yes
	
	core_meppen = {
		icon = mission_landsknecht_soldier
		required_missions = { }
		position = 1
		
		provinces_to_highlight = {
			OR = {
				province_id = 56 # Osnabrück
				province_id = 86 # Münster
			}
			NOT = { controlled_by = ROOT }
		}
		
		trigger = {
			AND = {
				controls = 56 # Osnabrück
				controls = 86 # Münster
			}
		}
		
		effect = {
			86 = { # Münster
				add_core = ROOT
			}
			56 = { # Osnabrück
				add_core = ROOT
			}
			2973 = { # Meppen
				add_core = ROOT
			}
			create_general = {
				tradition = 80
			}
		}
	}
}